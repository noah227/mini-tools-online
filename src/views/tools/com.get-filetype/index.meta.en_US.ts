import {TMetaStatic} from "@/types/common";

export const meta: TMetaStatic = {
    name: "get-filetype",
    text: "Get FileType",
    icon: "get-filetype",
    description: "Get the real type of the file",
    devOnly: true,
    faqList: [
        {
            title: "Get file type based on file"
        }
    ]
}