import {TMetaStatic} from "@/types/common";

export const meta: TMetaStatic = {
    name: "fast-schema",
    text: "fast schema",
    icon: "fast-schema",
    description: "Generate json schema fastly",
    content: {
        buttons: {
            clearInput: "Clear Input"
        },
        checks: {
            evalObject: "eval object"
        },
        placeholders: {
            addContent: "Input json content here or drag json file to this place"
        }
    }
}