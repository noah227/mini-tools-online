import {TMetaStatic} from "@/types/common";

export const meta: TMetaStatic = {
    name: "fast-schema",
    text: "fast schema",
    icon: "fast-schema",
    description: "快速json schema生成",
    content: {
        buttons: {
            clearInput: "清空输入"
        },
        checks: {
            evalObject: "eval对象"
        },
        placeholders: {
            addContent: "在此处输入json内容或拖放json文件到此处"
        }
    }
}